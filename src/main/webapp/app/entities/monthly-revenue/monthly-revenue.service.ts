import axios from 'axios';

import buildPaginationQueryOpts from '@/shared/sort/sorts';

import { type IMonthlyRevenue } from '@/shared/model/monthly-revenue.model';

const baseApiUrl = 'api/monthly-revenues';

export default class MonthlyRevenueService {
  public find(id: number): Promise<IMonthlyRevenue> {
    return new Promise<IMonthlyRevenue>((resolve, reject) => {
      axios
        .get(`${baseApiUrl}/${id}`)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public retrieve(paginationQuery?: any): Promise<any> {
    return new Promise<any>((resolve, reject) => {
      axios
        .get(baseApiUrl + `?${buildPaginationQueryOpts(paginationQuery)}`)
        .then(res => {
          resolve(res);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public delete(id: number): Promise<any> {
    return new Promise<any>((resolve, reject) => {
      axios
        .delete(`${baseApiUrl}/${id}`)
        .then(res => {
          resolve(res);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public create(entity: IMonthlyRevenue): Promise<IMonthlyRevenue> {
    return new Promise<IMonthlyRevenue>((resolve, reject) => {
      axios
        .post(`${baseApiUrl}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public update(entity: IMonthlyRevenue): Promise<IMonthlyRevenue> {
    return new Promise<IMonthlyRevenue>((resolve, reject) => {
      axios
        .put(`${baseApiUrl}/${entity.id}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public partialUpdate(entity: IMonthlyRevenue): Promise<IMonthlyRevenue> {
    return new Promise<IMonthlyRevenue>((resolve, reject) => {
      axios
        .patch(`${baseApiUrl}/${entity.id}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }
}
